import {DefaultTheme} from "react-native-paper";

const Theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: '#01aac1',
    secondary: '#111111',
  },
};

export default Theme;